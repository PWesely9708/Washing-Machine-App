# Washing-Machine-App

React Native (Expo + expo-router) app.

## Prereqs
- Node LTS
- Expo CLI
- Android Studio (SDK + emulator) or a physical Android device

## Quick Start
```bash
npm install
npx expo start
